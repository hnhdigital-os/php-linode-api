endpoint: linode/instances/%s/ips/%s
description: Manage a particular IP Address associated with this Linode.
url: https://developers.linode.com/v4/reference/endpoints/linode/instances/$id/ips/$ip_address
load-method: get

parameters:

    linode_id:
      type: int

    ip_address:
      type: string

get:

  get:
    description: Returns information about this IPv4 or IPv6 Address.
    url: https://developers.linode.com/v4/reference/endpoints/linode/instances/$id/ips/$ip_address#GET
    endpoint: 

put:

  update:
    description: Update this IP Address
    url: https://developers.linode.com/v4/reference/endpoints/linode/instances/$id/ips/$ip_address#PUT
    endpoint: 
    attributes: true

delete:

  delete:
    description: Delete a public IPv4 address associated with this Linode
    url: https://developers.linode.com/v4/reference/endpoints/linode/instances/$id/ips/$ip_address#DELETE
    endpoint: 