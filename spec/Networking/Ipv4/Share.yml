endpoint: networking/ipv4/share
url: 'https://developers.linode.com/api/v4#tag/Networking-Ipv4-Share'
load-method: get
post:
  shareIPs:
    summary: 'Configure IP Sharing'
    description: "Configure shared IPs.  A shared IP may be brought up on a Linode other than the one it lists in its response.  This can be used to allow one Linode to begin serving requests should another become unresponsive.\n"
    url: 'https://developers.linode.com/api/v4#operation/shareIPs'
    endpoint: ''
    attributes: true
    optional: true
    search: true
    factory:
      class: Share\I\P
      parameters:
        - id
