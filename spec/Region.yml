endpoint: regions/%s
description: Return a particular region.
url: https://developers.linode.com/v4/reference/endpoints/regions
load-method: get
fillable: true

parameters:

    region_id:
      type: integer

get:

  get:
    url: https://developers.linode.com/v4/reference/endpoints/regions/$id#GET
    description: Returns information about this region.
    endpoint: 