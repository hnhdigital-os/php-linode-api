endpoint: regions/%s
url: 'https://developers.linode.com/api/v4#tag/Regions'
load-method: get
parameters:
  region_id: {  }
get:
  get:
    summary: 'View Region'
    description: "Returns a single Region.\n"
    url: 'https://developers.linode.com/api/v4#operation/getRegion'
    endpoint: ''
    parameters:
      region_id:
        description: 'ID of the Region to look up.'
        required: true
        type: string
        self: true
