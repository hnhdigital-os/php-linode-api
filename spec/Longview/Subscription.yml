endpoint: longview/subscriptions/%s
url: 'https://developers.linode.com/api/v4#tag/Longview-Subscriptions'
load-method: get
parameters:
  subscription_id: {  }
get:
  get:
    summary: 'View Longview Subscription'
    description: "Returns a single LongviewSubscription object.  This is a public endpoint and requires no authentication.\n"
    url: 'https://developers.linode.com/api/v4#operation/getLongviewSubscription'
    endpoint: ''
    parameters:
      subscription_id:
        description: 'The Longview Subscription to look up.'
        required: true
        type: string
        self: true
